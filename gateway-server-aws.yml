eureka:
  client:
    serviceUrl:
      defaultZone: http://ip-10-103-13-170.eu-west-1.compute.internal:5100/edgeeureka/eureka/
server:
  port: 5200
  
logging:
  level:
    org.springframework: TRACE
  
spring:
  cloud:
    gateway:
      routes:
        - id: microservice-odp-aws
          uri: lb://MICROSERVICE-ODP-AWS
          predicates:
            - Path=/OdpDatasetService/**, /UserService/**, /RedmapService/**
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true

management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
        - '*'
